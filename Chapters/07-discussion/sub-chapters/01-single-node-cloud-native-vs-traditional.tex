\section{Cloud-native and traditional engines on a single node}
\label{discussion:single-node-cloud-native-vs-traditional}

% RQ1 interpretation. Reads Section~\ref{results:single-machine-query-performance}.
% Goal: explain WHY the single-node orderings came out as they did; Results only reports them.
%
% Points to cover:
% - PostGIS recorded the lowest run-time in every single-machine cell
%   (Figure~\ref{fig:single-machine-wall-clock-time}, Table~\ref{tab:results-achieved-n}),
%   often by orders of magnitude. Interpret as a tuned, server-resident, R-tree-indexed engine
%   answering from warm local state, against the per-request latency and CPU overhead that
%   storage--compute decoupling imposes
%   \parencite{dageville2016_snowflake_elastic_data_warehouse,pang2024_storage_disaggregated_databases}.
% - DuckDB's cloud-native advantage showed up as transfer footprint, not speed: near-constant
%   bytes received across patterns (Figure~\ref{fig:single-machine-bytes-transferred}), so
%   per-row transfer fell as cardinality grew (Figure~\ref{fig:bytes-vs-cardinality}). Tie to
%   range-request access and column/row-group pruning that read only the needed byte intervals
%   \parencite{maso2023_ogc_cog_standard,fielding2022_rfc9110_http_semantics,duckdb2025_parquet_overview}.
% - Directional contrast: PostGIS received volume rose with result cardinality (server returns
%   the result set) while DuckDB's stayed flat (reads a fixed format footprint),
%   Figure~\ref{fig:bytes-vs-cardinality}. Note the host-boundary byte count means different
%   things per architecture (Section~\ref{results:consistency-of-winners}), so this is a
%   directional reading, not a ranking.
% - Counterintuitive headline: a cloud-native engine lost on wall-clock time throughout. Frame
%   as object-storage round-trips dominating sub-second queries; the format-design advantage
%   (fewer bytes) did not convert into time against a warm, indexed local engine.
% - Cost/time decoupling: DuckDB won on cost at the small tier for kNN and bounding-box while
%   losing on time (Figure~\ref{fig:single-machine-operational-cost},
%   Figure~\ref{fig:single-machine-cost-time-quadrant}). Interpret via per-resource metering
%   (Section 2.1.4) \parencite{mell2011_nist_definition_of_cloud_computing}.
% - GeoPandas over Shapefile: slowest measured on kNN
%   (Figure~\ref{fig:single-machine-cross-pattern}); attribute to the absence of a usable
%   spatial index, so search degrades toward a scan \parencite{guttman1984_rtree_dynamic_index}.
%   Keep brief.
% - Caveat (do not over-claim): the Shapefile path is exercised through GeoPandas, so it reads
%   as the GeoPandas+Shapefile stack, not the format alone -- carry forward to
%   Section~\ref{discussion:reach-and-limits}.