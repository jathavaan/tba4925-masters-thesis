\section{Consistency of winners (RQ3)}
\label{results:consistency-of-winners}

% ORIENTING COMMENT (section intro prose -- TODO below). One paragraph, no
%   numbers: RQ3 is an analysis over the RQ1/RQ2 outputs, not a new measurement
%   (Section~\ref{research-design-and-methodology:statistical-analysis:answering-rq3-consistency-across-patterns}).
%   Report which configuration wins per (workload pattern, outcome dimension),
%   how separable the rankings are, and the magnitude of the effects. Results
%   only; interpretation is deferred to the Discussion (Chapter~\ref{ch:discussion}).
% IMPORTANT: the Friedman chi-squared omnibus test, once available, is reported as a
%   SENTENCE plus its statistic in the prose below -- it is NOT a float. The statistic
%   currently on file is the wrong test (see the statistical-separability subsection): the
%   omnibus sentence is MISSING pending a recomputed cross-cell Friedman. Do not wrap it in a
%   figure or table when it lands.

% TODO (prose): section-opening paragraph per the orienting comment above.

\subsection{Winners across dimensions}
\label{results:consistency-of-winners:winners-across-dimensions}

% TODO (prose): factual readout only -- which configuration wins each cell and
% where the winner changes across patterns/tiers/dimensions. Refer to
% Figure~\ref{fig:winners-matrix}. No interpretation (-> Discussion).
% OBSERVATIONS (from the per-cell winners data): on RUN-TIME, PostGIS wins every
%   single-machine cell; on the national join, Apache Sedona wins the medium and large
%   tiers. On BYTES, the local Shapefile path wins the small single-machine cells it ran
%   (its network transfer is approximately zero), with PostGIS or DuckDB winning the large
%   cells. On COST, the winner is mixed (DuckDB on some small cells, PostGIS on the large
%   single-machine cells, Sedona on the medium and large join). FLAG THE TIES: at the join
%   small tier the PostGIS-vs-Sedona difference on time and bytes is NOT statistically
%   significant, and the join medium tier is likewise not significant -- report these cells
%   as statistically tied rather than naming a winner. The chapter must say "tied" where a
%   test is not significant.

\begin{figure}[H]
    \centering
    % WHAT IT SHOWS: winners matrix / heatmap. Rows = workload pattern x size tier;
    %   columns = outcome dimension (data transferred, wall-clock time, operational
    %   cost). Each cell is coloured by the WINNING configuration.
    % CAPTION MUST STATE: the row/column layout; that cell colour is the winning
    %   configuration; and that each cell's winner is taken among the configurations
    %   that ran it -- Shapefile ran the small tier only (not a candidate at the large
    %   tier), while kNN DID run at both tiers for DuckDB and PostGIS.
    % DESCRIPTIVE ONLY: read the winners; no interpretation (-> Discussion).
    \includegraphics[width=0.85\textwidth]{Chapters/06-results/sub-chapters/figures/06-winners-matrix.pdf}
    \caption[Consistency of winners across dimensions]{Winning configuration for
    every workload pattern and size tier (rows) on each outcome dimension
    (columns: data transferred, wall-clock time, operational cost). Cell colour
    encodes the winning configuration using the thesis palette. Cells that did not
    run by design carry no winner and are marked accordingly.}
    \label{fig:winners-matrix}
\end{figure}

% TODO (prose): factual readout only -- trace each system's polyline across the four
% outcome axes and name where polylines cross (outcome inversions). Refer to
% Figure~\ref{fig:outcome-parallel-coordinates}. No interpretation (-> Discussion).
% OBSERVATIONS (from the geometric-mean normalized values): the polylines cross between
%   axes -- PostGIS and Sedona lead on time and cost but trail the local Shapefile path on
%   bytes, while the local path leads on bytes but trails badly on time. Name these
%   crossings as outcome inversions (a system that leads one dimension trails another);
%   do not interpret why.

\begin{figure}[H]
    \centering
    % WHAT IT SHOWS: parallel-coordinates plot. One polyline per system family
    %   (DuckDB, PostGIS, GeoPandas+Shapefile, Sedona) across four outcome axes --
    %   wall-clock time, bytes, operational cost, and mean rank. Each axis is scaled
    %   independently with the best (lowest) value at the bottom; lines that cross
    %   between axes are outcome inversions, surfaced in a single frame.
    % CAPTION MUST STATE: that each polyline is one system; the four outcome axes;
    %   that the time/bytes/cost axes use the geometric-mean normalized values of
    %   Table~\ref{tab:geomean-normalized-performance}; that axes are independently
    %   scaled (best at bottom) with their real extremes annotated; that crossings
    %   denote inversions.
    % DESCRIPTIVE ONLY: report the crossings; no interpretation (-> Discussion).
    \includegraphics[width=0.9\textwidth]{Chapters/06-results/sub-chapters/figures/06-outcome-parallel-coordinates.pdf}
    \caption[Per-system outcomes across dimensions]{Per-system outcomes across the
    four outcome dimensions -- wall-clock time, bytes transferred, operational cost,
    and mean rank -- drawn as a parallel-coordinates plot with one polyline per
    system. The time, bytes, and cost axes use the geometric-mean normalized values
    of Table~\ref{tab:geomean-normalized-performance}; each axis is scaled
    independently with the best (lowest) value at the bottom and its real extremes
    annotated. Polylines that cross between axes mark outcome inversions, where a
    system that leads on one dimension trails on another. Colour encodes the system
    using the thesis palette.}
    \label{fig:outcome-parallel-coordinates}
\end{figure}

\subsection{Aggregate normalized performance}
\label{results:consistency-of-winners:aggregate-normalized-performance}

% TODO (prose): factual readout only -- the geometric-mean normalized score per
% configuration. Refer to Table~\ref{tab:geomean-normalized-performance}. No
% interpretation (-> Discussion).
% COVERAGE CAVEAT (state it): the single-machine configurations (DuckDB, PostGIS, local
%   Shapefile) are normalized over the single-machine query patterns, while Apache Sedona is
%   normalized over the national-join workloads; the per-workload "best" baseline therefore
%   differs and the configurations are not all measured on the same workload set. Report the
%   scores and this scope; do not read the table as a single common ranking.

\begin{table}[H]
    \centering
    % WHAT IT SHOWS: geometric-mean normalized performance. For each configuration,
    %   the geometric mean of its per-workload slowdown relative to the best
    %   configuration on that workload (1.0 = best). One value per configuration
    %   per outcome dimension.
    % CAPTION MUST STATE: the normalization (per-workload slowdown vs. the best,
    %   then geometric mean across workloads, so 1.0 is best); and cite the
    %   geometric-mean rationale (Fleming--Wallace).
    % DATA: single-sourced via \inputtablebody from the analysis fragment
    %   (tables/06-rq3-geomean.tex) -- do NOT edit rows by hand. Header, caption, label stay here.
    \setlength{\tabcolsep}{8pt}
    \renewcommand{\arraystretch}{1.2}
    \begin{tabular}{@{}l r r r@{}}
        \toprule
        \textbf{Configuration} & \textbf{Time} & \textbf{Bytes} & \textbf{Cost} \\
        \midrule
        \inputtablebody{Chapters/06-results/sub-chapters/tables/06-rq3-geomean.tex}
        \bottomrule
    \end{tabular}
    \caption[Geometric-mean normalized performance]{Geometric-mean normalized
    performance per configuration. For each workload, a configuration's outcome is
    expressed as its slowdown relative to the best configuration on that workload
    (\num{1.0} denotes the best), and the values are aggregated across workloads by the
    geometric mean, the appropriate summary for normalized ratios
    \parencite{fleming1986_how_not_to_lie_with_statistics}. Lower is better.}
    \label{tab:geomean-normalized-performance}
\end{table}

\subsection{Ranking stability}
\label{results:consistency-of-winners:ranking-stability}

% TODO (prose): factual readout only -- whether configuration rankings hold across
% tiers, and where inversions occur. Refer to Figure~\ref{fig:ranking-stability}.
% NOTE: the figure is faceted per workload pattern, with rank (1 = fastest) against tier;
%   the single-machine patterns span two tiers (slopegraph) and the national join spans
%   three (bump). Report whether each line stays flat (stable rank) or crosses (inversion).
% No interpretation (-> Discussion).

\begin{figure}[H]
    \centering
    % WHAT IT SHOWS: ranking-stability slopegraph / bump chart. Rank (y) against
    %   size tier (x), one line per configuration, faceted or coloured per
    %   workload; lines that cross expose rank inversions across tiers.
    % CAPTION MUST STATE: the axes (rank vs. tier); that each line is a
    %   configuration; that crossings are rank inversions.
    % NOTE: this is a SLOPEGRAPH where only two RQ1 tiers exist (small, large),
    %   and a BUMP chart only where three tiers exist (the join: small/medium/large).
    % DESCRIPTIVE ONLY: report the crossings; no interpretation (-> Discussion).
    \includegraphics[width=0.85\textwidth]{Chapters/06-results/sub-chapters/figures/06-ranking-stability.pdf}
    \caption[Ranking stability across tiers]{Configuration rank as a function of
    size tier, one line per configuration. Lines that cross mark rank inversions
    between tiers. The chart is a slopegraph between the two single-machine tiers
    and a bump chart across the three tiers of the distributed join. Line colour
    encodes configuration using the thesis palette.}
    \label{fig:ranking-stability}
\end{figure}

% TODO (prose): add a sentence contrasting the per-dimension rank ordering (portable)
% with the absolute geomean-normalized magnitudes (not portable), referring to
% Figure~\ref{fig:rank-portability} and Table~\ref{tab:geomean-normalized-performance}.
% No interpretation (-> Discussion).

\begin{figure}[H]
    \centering
    % WHAT IT SHOWS: two panels over the three outcome dimensions (time, bytes, cost).
    %   (left) per-dimension RANK of each system as a slopegraph (1 = best); (right)
    %   the same systems' ABSOLUTE geometric-mean-normalized magnitude per dimension
    %   on a log axis (1.0 = the dimension's best).
    % CAPTION MUST STATE: the shared systems and three dimensions; that the left panel
    %   is rank (1 = best) and the right is the geomean-normalized magnitude (log, 1.0
    %   = best) of Table~\ref{tab:geomean-normalized-performance}; that it is distinct
    %   from the rank-vs-tier ranking-stability figure.
    % DESCRIPTIVE ONLY: report the rank crossings and the magnitude spread; no
    %   interpretation.
    \includegraphics[width=\textwidth]{Chapters/06-results/sub-chapters/figures/06-rank-portability.pdf}
    \caption[Rank portability versus absolute magnitude]{Per-system outcomes across
    the three dimensions -- time, bytes, and cost -- in two panels. The left panel is
    a slopegraph of each system's rank on each dimension (\num{1} = best), so a
    near-flat line is a portable ordering; the right panel shows the same systems'
    absolute geometric-mean-normalized magnitude per dimension on a logarithmic axis
    (\num{1.0} = the dimension's best), from
    Table~\ref{tab:geomean-normalized-performance}. The figure is distinct from the
    rank-versus-tier ranking-stability figure (Figure~\ref{fig:ranking-stability}).
    Colour encodes the system using the thesis palette.}
    \label{fig:rank-portability}
\end{figure}

% TODO (prose): add a sentence on how each configuration's wall-clock ordering across
% the query patterns changes at a fixed tier (which leads on which pattern),
% referring to Figure~\ref{fig:single-machine-cross-pattern}. No interpretation.

\begin{figure}[H]
    \centering
    % WHAT IT SHOWS: cross-pattern single-machine comparison, one panel per size tier.
    %   x = workload pattern, grouped bars per configuration, y = wall-clock minimum
    %   (s, log) with the 95% bootstrap CI as a whisker.
    % CAPTION MUST STATE: the per-tier panels; the workload-pattern x-axis and grouped
    %   bars per configuration; the log y-axis; the minimum estimator with 95%
    %   bootstrap CI; that the Shapefile configuration ran the small tier only and is
    %   labelled did-not-run-by-design at the large tier.
    % NOTE: this is the cross-pattern re-cut of the pattern-by-tier wall-clock grid
    %   (Figure~\ref{fig:single-machine-wall-clock-time}); the caption cross-references
    %   it so the two are not read as duplicates.
    % DESCRIPTIVE ONLY: report the per-pattern orderings; no interpretation.
    \includegraphics[width=\textwidth]{Chapters/06-results/sub-chapters/figures/06-single-machine-cross-pattern.pdf}
    \caption[Cross-pattern single-machine comparison by tier]{Wall-clock minimum of
    each single-machine configuration across the query patterns, one panel per size
    tier, on a logarithmic axis with the \num{95}\% bootstrap confidence interval as a
    whisker. Bars are grouped by configuration within each workload pattern. It re-cuts
    the pattern-by-tier wall-clock grid (Figure~\ref{fig:single-machine-wall-clock-time})
    to read run-time across the query patterns at a fixed tier, so it is a cross-pattern
    view of that grid rather than a duplicate. The Shapefile configuration ran at the
    small tier only and is labelled as did-not-run-by-design at the large tier rather
    than left as a silent gap. Colour encodes configuration using the thesis palette.}
    \label{fig:single-machine-cross-pattern}
\end{figure}

\subsection{Statistical separability}
\label{results:consistency-of-winners:statistical-separability}

% TODO (prose): factual readout only. The Friedman OMNIBUS sentence is currently MISSING:
%   the statistic on file is the WRONG TEST for RQ3. It was computed on a single cell
%   (point-in-polygon, small tier) blocking by ITERATION (chi-squared 2564, df 2, 3 configs,
%   1282 blocks), which only tests whether the three single-machine configs differ within
%   that one cell. RQ3's cross-pattern omnibus
%   (Section~\ref{research-design-and-methodology:statistical-analysis:answering-rq3-consistency-across-patterns})
%   must instead block by workload x tier CELL across configurations. Do NOT report the
%   on-file chi-squared/df/p as the RQ3 omnibus; leave the sentence out until the correct
%   cross-cell Friedman is recomputed in the analysis repo, then state its statistic in prose
%   (NOT a float). The per-comparison separability and effect magnitudes below are correct
%   and unaffected: refer to Figure~\ref{fig:pairwise-significance-cliques} and
%   Figure~\ref{fig:vargha-delaney-forest}, and the full pairwise table in the appendix
%   (Table~\ref{tab:appendix-pairwise-statistics}).
% No interpretation (-> Discussion).

\begin{figure}[H]
    \centering
    % WHAT IT SHOWS: pairwise-significance / clique diagram. A horizontal mean-rank
    %   axis with the configurations placed at their mean ranks; connectors join
    %   configurations whose pairwise difference is NOT significant under the
    %   Holm-adjusted Wilcoxon tests (i.e. non-significance cliques).
    % CAPTION MUST STATE: the mean-rank axis; that connectors are non-significance
    %   cliques from Holm-adjusted pairwise Wilcoxon tests; cite the
    %   Wilcoxon--Holm--clique lineage.
    % IMPORTANT: do NOT call this a "critical-difference diagram" -- that term
    %   implies the Nemenyi post-hoc test, which is NOT used here. Describe the
    %   connectors as non-significance cliques.
    % DESCRIPTIVE ONLY: report which configs share a clique; no interpretation.
    \includegraphics[width=0.8\textwidth]{Chapters/06-results/sub-chapters/figures/06-pairwise-significance-cliques.pdf}
    \caption[Pairwise significance and non-significance cliques]{Configurations
    placed along a mean-rank axis, with connectors joining configurations whose
    pairwise difference is not statistically significant under Holm-adjusted
    Wilcoxon tests \parencite{laaber2019_software_microbenchmarking_cloud,holm1979_simple_sequentially_rejective}.
    The connectors denote non-significance cliques; the layout follows the
    mean-rank diagram convention of \textcite{demsar2006_statistical_comparisons_classifiers}
    but does not use the Nemenyi post-hoc test and is therefore not a
    critical-difference diagram.}
    \label{fig:pairwise-significance-cliques}
\end{figure}

\begin{figure}[H]
    \centering
    % WHAT IT SHOWS: Vargha--Delaney A_12 forest plot. A_12 effect size (x) for
    %   each KEY pairwise comparison (y), with a marker and CI per comparison, the
    %   0.5 no-effect line, and the arcuri2014 magnitude bands at 0.56 / 0.64 / 0.71.
    % CAPTION MUST STATE: that x is the A_12 effect size; the 0.5 no-effect line;
    %   the small/medium/large bands (0.56/0.64/0.71) and their source (arcuri2014).
    % SCOPE: restrict to the comparisons that carry the argument -- DuckDB vs.
    %   PostGIS for RQ1, and Sedona vs. the best single-node engine for RQ2 -- not
    %   every possible pair.
    % DESCRIPTIVE ONLY: report which bands the effects fall in; no interpretation.
    \includegraphics[width=0.95\textwidth]{Chapters/06-results/sub-chapters/figures/06-vargha-delaney-forest.pdf}
    \caption[Vargha--Delaney effect-size forest]{Vargha--Delaney $\hat{A}_{12}$
    effect size for the key pairwise comparisons -- DuckDB versus PostGIS for RQ1
    and Apache Sedona versus the best single-node engine for RQ2. The vertical line
    at \num{0.5} marks no effect, and the bands at \num{0.56}, \num{0.64}, and \num{0.71} mark the
    small, medium, and large thresholds \parencite{arcuri2014_hitchhiker_statistical_tests}.}
    \label{fig:vargha-delaney-forest}
\end{figure}

% TODO (prose): closing paragraph. Give an interpretation-free recap of the
% chapter -- which orderings held across patterns/tiers/dimensions and where they
% diverged -- WITHOUT explaining why. All interpretation is deferred to the
% Discussion (Chapter~\ref{ch:discussion}). Do not add a separate summary section.
