\section{Single-machine query performance (RQ1)}
\label{results:single-machine-query-performance}

% TODO: section-opening placeholder. Present the single-machine (single-node)
% configurations along the three outcome dimensions in order: data transferred
% first (Section~\ref{results:single-machine-query-performance:data-transferred}),
% then wall-clock time
% (Section~\ref{results:single-machine-query-performance:wall-clock-time}), then
% operational cost
% (Section~\ref{results:single-machine-query-performance:operational-cost}).
% Results only; interpretation is deferred to the Discussion
% (Chapter~\ref{ch:discussion}).
%
% Note: across the grid figures in this section, leave the large-tier Shapefile
% slot empty (the Shapefile configuration ran on the small tier only). Do not
% draw an empty bar, a zero, or any marker that would imply a missing or failed
% cell.

\subsection{Data transferred}
\label{results:single-machine-query-performance:data-transferred}

% TODO: prose placeholder describing the bytes-transferred results. Refer to
% Figure~\ref{fig:single-machine-bytes-transferred} and, optionally,
% Figure~\ref{fig:single-machine-bytes-vs-time}.

\begin{figure}[H]
    \centering
    % TODO: bytes grid. Rows = workload pattern, columns = size tier; within each
    % cell, grouped bars per configuration on a logarithmic y-axis with
    % confidence-interval whiskers. Plot bytes received and bytes sent separately
    % (two grids or two panels). Bars distinguished by configuration using the
    % thesis palette (\texttt{thesisteal}, \texttt{thesiscoral},
    % \texttt{thesissteel}, \texttt{thesisamber}). Leave the large-tier Shapefile
    % slot empty; do not imply a missing cell.
    \caption[Data transferred per single-machine configuration]{Bytes transferred
    by each single-machine configuration, arranged as a grid with workload
    patterns in rows and size tiers in columns. Within each cell, bars give the
    per-configuration volume on a logarithmic axis with confidence-interval
    whiskers; bytes received and bytes sent are shown separately. Bar colour
    encodes configuration using the thesis palette.}
    \label{fig:single-machine-bytes-transferred}
\end{figure}

\begin{figure}[H]
    \centering
    % TODO (optional): scatter of bytes transferred against wall-clock time, one
    % point per configuration x pattern x tier, exposing the transfer-vs-time
    % relationship. Points coloured by configuration using the thesis palette
    % (\texttt{thesisteal}, \texttt{thesiscoral}, \texttt{thesissteel},
    % \texttt{thesisamber}); logarithmic axes.
    \caption[Bytes transferred versus wall-clock time]{Relationship between bytes
    transferred and wall-clock time across single-machine configurations, with one
    point per configuration, workload pattern, and size tier. Point colour encodes
    configuration using the thesis palette.}
    \label{fig:single-machine-bytes-vs-time}
\end{figure}

\subsection{Wall-clock time}
\label{results:single-machine-query-performance:wall-clock-time}

% TODO: prose placeholder describing the wall-clock-time results. Refer to
% Figure~\ref{fig:single-machine-wall-clock-time}.

\begin{figure}[H]
    \centering
    % TODO: wall-clock-time grid, same layout as the bytes grid
    % (Figure~\ref{fig:single-machine-bytes-transferred}): rows = pattern,
    % columns = tier, grouped bars per configuration, logarithmic y-axis,
    % confidence-interval whiskers. Bars distinguished by configuration using the
    % thesis palette (\texttt{thesisteal}, \texttt{thesiscoral},
    % \texttt{thesissteel}, \texttt{thesisamber}). Leave the large-tier Shapefile
    % slot empty; do not imply a missing cell.
    \caption[Wall-clock time per single-machine configuration]{Wall-clock time of
    each single-machine configuration, in the same grid layout as the
    bytes-transferred figure: workload patterns in rows, size tiers in columns,
    per-configuration bars on a logarithmic axis with confidence-interval whiskers.
    Bar colour encodes configuration using the thesis palette.}
    \label{fig:single-machine-wall-clock-time}
\end{figure}

\subsection{Operational cost}
\label{results:single-machine-query-performance:operational-cost}

% TODO: prose placeholder describing the operational-cost results. Refer to
% Figure~\ref{fig:single-machine-operational-cost}.

\begin{figure}[H]
    \centering
    % TODO: stacked cost bars per configuration, faceted by workload pattern. Each
    % bar splits operational cost into a compute component and a transfer
    % component, the two distinguished using the thesis palette
    % (\texttt{thesissteel} compute, \texttt{thesiscoral} transfer). Leave the
    % large-tier Shapefile slot empty; do not imply a missing cell.
    \caption[Operational cost per single-machine configuration]{Operational cost
    of each single-machine configuration, faceted by workload pattern. Each bar is
    stacked into its compute and transfer components, the two distinguished using
    the thesis palette.}
    \label{fig:single-machine-operational-cost}
\end{figure}
