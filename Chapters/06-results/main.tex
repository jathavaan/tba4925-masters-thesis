% File: Chapters/06-results/main.tex
% Lead paragraph (TODO): chapter-opening placeholder. State what ran and which
% configuration x pattern x tier cells completed; note the Shapefile
% small-tier-only asymmetry (the Shapefile configuration was exercised on the
% small tier only, so its large-tier cell is absent by design rather than
% missing); and state that interpretation of these results is deferred to the
% Discussion (Chapter~\ref{ch:discussion}). Report no numbers here: this chapter
% presents results without interpreting them.

% ============================ DRAFTING GUIDANCE (whole chapter) ============================
% Authoritative data: the analysis repository (sibling `doppa-analytics/`): its generated
%   table fragments, the decision-quadrant and omnibus exports, and the figure-generating
%   code. Every number in the prose must trace to a placed float or its source data; where a
%   skeleton number and the data disagree, the data wins. Numbers that match are confirmed;
%   the only stale values found were in the distributed and summary-statistics CI columns,
%   now single-sourced via \inputtablebody from the analysis fragments (see those tables).
% INCLUDE in the prose: magnitudes with units; for each headline pair the point estimate --
%   the MINIMUM estimator for run-time, the MEDIAN for transferred bytes -- with its
%   \num{95}\% PERCENTILE bootstrap confidence interval (10{,}000 resamples); significance and
%   effect direction stated in words; comparative observations, crossovers, and anomalies;
%   the phrase "statistically tied" wherever a test is not significant; and the
%   "did not run by design" framing for the kNN-large and Shapefile-beyond-small cells.
% EXCLUDE from the prose: mechanism or cause, recommendation, outside-literature comparison,
%   any answer/verdict/overall-winner or resolution of a research question, forward-references
%   to later chapters, AND every internal data-source identifier (table-fragment, notebook,
%   or CSV file names, column names). Those identifiers live in these comments as provenance
%   ONLY; the prose cites floats via Figure~\ref / Table~\ref and methods via Section~\ref.
% LEAN STATS: the prose carries the point estimate + CI + worded significance for the few
%   anchoring comparisons; the full pairwise matrix stays in the appendix
%   (Table~\ref{tab:appendix-pairwise-statistics}). Defer all interpretation to the Discussion
%   (Chapter~\ref{ch:discussion}).
% ==========================================================================================

\input{Chapters/06-results/sub-chapters/01-overview-and-measurement-quality}
\input{Chapters/06-results/sub-chapters/02-single-machine-query-performance}
\input{Chapters/06-results/sub-chapters/03-distributed-join-and-scaling}
\input{Chapters/06-results/sub-chapters/04-consistency-of-winners}
